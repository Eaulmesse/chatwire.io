services:
  api:
    build: .
    # Force l'utilisation du polling via nodemon si nest start --watch échoue
    command: npm run start:dev 
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "3000:3000"